apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: redis
  namespace: {{ .Values.namespace }}
spec:
  selector:
    istio: ingressgateway-internal
  servers:
  - port:
      number: 6379
      name: redis
      protocol: TCP
    hosts:
    - {{ .Values.redisHost }}

